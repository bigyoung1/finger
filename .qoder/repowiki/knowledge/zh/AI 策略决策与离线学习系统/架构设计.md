- **数据层**：`weights.json` 存储全局通用权重，`skills/*.md` 定义各角色的专属 JSON 权重及 Markdown 格式的技能攻略。
- **知识层**：`knowledge.md` 作为动态知识库，记录通用原则、角色应对要点及历史对局复盘经验。
- **逻辑层**：`preTrain.js` 为核心脚本，负责解析 `log/` 目录下的对战日志，通过调用 MiniMax API 批量提炼新规则并追加至知识库。
- **依赖方向**：决策逻辑依赖静态权重文件与动态知识库；训练脚本依赖外部日志输入与 LLM API。