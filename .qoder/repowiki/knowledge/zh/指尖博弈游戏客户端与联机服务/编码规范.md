- 采用两步点击交互模式：第一步选择己方动作源，第二步选择敌方目标以触发 `doAttack2`。
- 渲染优化采用 `requestAnimationFrame` 批处理机制，通过 `_renderPending` 标志位避免同一帧内的重复 DOM 操作。
- 联机同步遵循“房主权威”原则，由房主分配角色控制权 (`charControl`) 并广播房间状态，客户端根据控制权限锁定输入。
- 视觉特效（VFX）通过 `_damageQueue` 和 `_healQueue` 队列积累状态变更，在渲染周期结束时统一触发差量动画。