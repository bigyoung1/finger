- **表现层 (Presentation)**: `css/game2.css` 定义了完整的UI布局，包括设置面板、战场网格、角色卡片（含血量、手牌、状态Buff）及联机大厅样式。
- **数据持久化 (Data Persistence)**: `log/` 目录以纯文本形式归档对战数据，文件名遵循 `log_[阵容]_[时间].txt` 或 `train_[ID]_[阵营]_[时间].txt` 规范。
- **内容结构**: 日志文件详细记录了从初始化、每回合动作（如“碰”数字变化）、技能触发（如“双二护盾”、“破军组合”）到最终胜负判定的全链路事件流。